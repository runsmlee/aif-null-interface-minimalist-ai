/**
 * Application-wide constants.
 * Centralizes hardcoded strings and configuration values to ensure
 * consistency across components and ease future updates.
 */

/** Application display name — used in headers, titles, and exports. */
export const APP_NAME = "Null Interface";

/** Semantic version displayed in the header. */
export const APP_VERSION = "v2.7";

/** Storage key prefix for localStorage persistence. */
export const STORAGE_KEY = "null-interface-messages";

/** Export file name prefix. */
export const EXPORT_PREFIX = "null-interface";
